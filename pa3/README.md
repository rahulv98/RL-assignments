#Q1 - run run.py to generate all the data(will be stored in results) required for ploting
    - run plot.py to generate all the plots used in report

#Q2 - run simple_dqn_cartpole_skeleton.py to generate all the data required for ploting best learning curve
    - run experiments.py tp generate data required for plotting curves for all other experiments 
    - all the data generated is stored in results folder.
    - run plot.py to generate all the plots used in report
